% =======================================================================
\section{Methods}

\subsection{Data}
\label{sec:data}

The primary single-match analysis uses SkillCorner open broadcast
tracking data \citep{SkillCorner2024}, match ID 1996435 (Sydney FC
versus Adelaide United, A-League 2024/25), at 10 frames per second. We
retain only frames with complete 22-player coverage, yielding
43{,}531 frames (approximately 72 minutes at this sampling rate).
Positions are $(x, y)$ coordinates in metres on a standard football
pitch model. The multi-match sample comprises that match and nine
further A-League matches from the same repository, each with
approximately 39{,}000--49{,}000 complete frames (436{,}648 frames
in total). SkillCorner match IDs and fixtures are listed in
Supplementary Table~\ref{tab:matches}.

Consecutive complete frames share nearly the same configuration, so the
full stream is not treated as a sequence of independent snapshots. For
prevalence and organisational-level summaries we subsample. Write $t$
for the time of a complete frame in the native stream. The operational
rule is the same on every match: list complete 22-player frames, set
the stride to $\lfloor N/150\rfloor$, and retain 150 frames. Write
$\tilde{t}$ for a time retained by that rule, and $\tilde{T}$ for the
set of such times on one match, so $|\tilde{T}|=150$. On the primary
match $N = 43{,}531$, so the stride is 290 frames ($\approx 29$~s).
Across ten matches the sampled times give 1{,}500 frames. The design
limits temporal pseudo-replication. It does not treat the
competitive-dependence inference problem named in
Section~\ref{sec:background}, and it does not resolve formation or
dispersion of loops between samples. Event association uses the
native-rate stream $t$ and is a construct-validity check, not a
dynamical model. Supplementary Figure~\ref{fig:acf} is an
autocorrelation diagnostic on the primary match at 1~Hz; it does not
choose $\tilde{T}$.

\subsection{Hierarchical clustering}
\label{sec:clustering}

On this corpus the named organisational levels of
Section~\ref{sec:scaleconflation} (individual, small group, envelope)
are instantiated as individual, tactical, and team. The class names
those levels by cardinality. The cutoffs $\delta$ that select them are
read from the sweep in Section~\ref{sec:cutoffselection}. They are not
assumed in metres.

Figure~\ref{fig:pipeline} summarises the pipeline of
Sections~\ref{sec:clustering}--\ref{sec:cycles}, applied at the
sampled times $\tilde{t}\in\tilde{T}$ of Section~\ref{sec:data}. The
input is the point cloud
$P(\tilde{t}) = \{p_1(\tilde{t}), \dots, p_{22}(\tilde{t})\}
\subset \mathbb{R}^2$.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig1_pipeline_schematic}
\caption{Analysis pipeline (schematic geometry; each panel shows the
step and its output).
\textbf{(a)}~Colour-coded local groups in
\texorpdfstring{$P(\tilde{t})$}{}
(left, 2\texorpdfstring{$\times$}{}2 layout) with dashed
\texorpdfstring{$\delta$}{}-disks and a representative gap
\texorpdfstring{$<\delta$}{}; centroids in
\texorpdfstring{$\tilde{P}(\tilde{t})$}{} (right column) linked by
colour-matched dashed lines (Section~2.2).
\textbf{(b)}~Vietoris--Rips (VR) on
\texorpdfstring{$\tilde{P}(\tilde{t})$}{}
truncated at \texorpdfstring{$\varepsilon_{\max}$}{}
(equation~\eqref{eq:adaptive}; Section~2.4).
\textbf{(c)}~Finite \texorpdfstring{$H_1$}{} birth--death pairs.
\textbf{(d)}~Graph-cycle proxy for an \texorpdfstring{$H_1$}{} pair
(Section~2.5). Figure~\ref{fig:cycles} shows the cycle step on real
tracking data.}
\label{fig:pipeline}
\end{figure}

For a cutoff distance $\delta > 0$, single-linkage hierarchical
clustering partitions $P(\tilde{t})$ into clusters $C_1, \dots, C_k$
such that every pair of points within a cluster is connected by a chain
of pairwise distances not exceeding $\delta$. The reduced point cloud
is the set of cluster centroids
\[
\tilde{P}(\tilde{t})=\left\{\bar{c}_j : \bar{c}_j=\frac{1}{|C_j|}\sum_{p\in C_j} p,\ j=1,\dots,k\right\},
\]
and persistent homology is computed on $\tilde{P}(\tilde{t})$ rather
than on $P(\tilde{t})$.

We use single-linkage throughout: clusters are chains of points
connected by pairwise distances at most $\delta$. Complete-linkage and
Ward's method are not used; their effect on $H_1$ counts is examined in
Section~\ref{sec:limitations}.

Team identity is not used as an input. The pipeline can be run
independently on a labelled subset of $P(\tilde{t})$; that decomposition is not
performed here.

\subsection{Cutoff selection}
\label{sec:cutoffselection}

The class names the three organisational levels by cardinality. The
sweep reads the metres from the pooled
\texorpdfstring{$H_0(\delta)$}{} curve on this corpus. Quality metrics
are shown as a check. They are not the selector.

The inputs are fixed before the run. A complete frame has $n=22$
agents. Clustering is single-linkage. Team labels are unused. The grid
is $\delta\in[0.25, 40.0]$~m at $0.25$~m steps (160 points). Every
complete 22-player frame of the ten matches in
Supplementary Table~\ref{tab:matches} is used (436{,}648 frames). There
is no random window draw and no mixing of epoch lengths.

Let $\overline{H_0}(\delta)$ be the mean cluster count at cutoff
$\delta$, pooled over those frames. Let $P(k\ge 4;\delta)$ be the
fraction of frames with at least four clusters. The selector is then:

\begin{itemize}
\item individual: largest $\delta$ with $\overline{H_0}(\delta)\ge n-3=19$;
\item tactical: $\delta$ minimising
$|\overline{H_0}(\delta)-5|$, among candidates with
$P(k\ge 4;\delta)\ge 0.5$;
\item team: smallest $\delta$ with $\overline{H_0}(\delta)\le 2$.
\end{itemize}
Ties take the smaller $\delta$. Those three targets are roster
statements. ``Mostly resolved agents'' is $n-3$. ``A handful of
coordinating groups, loop-feasible'' is 5, with $k\ge 4$. ``At most two
envelopes'' is 2.

On this corpus the inversion returns $2.75$~m, $11.75$~m, and
$23.0$~m. Pooled mean $H_0$ at those cutoffs is $19.31$, $5.06$, and
$1.98$. Inverting each match separately gives
$2.80\pm 0.20$~m, $11.80\pm 0.35$~m, and $22.98\pm 0.72$~m
(mean $\pm$ s.d.\ across matches). Acceptance requires every match's
mean $H_0$ at the pooled cutoff to lie in $[15, 22]$, $[4, 10]$, and
$[1, 2.5]$ respectively. All ten matches pass.

Calinski--Harabasz, silhouette, and Shannon entropy of cluster sizes
are recorded on a deterministic 1~Hz subset (every 10th complete
frame). Silhouette is omitted when $k=1$; it is not coded as 0.
Silhouette local maxima are reported as characteristic separations.
They are alternatives, not adopted cutoffs.
Figure~\ref{fig:cutoff} shows the sweep.
Section~\ref{sec:sensitivity} reports the sensitivity of $H_1$
detection to the tactical choice.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig2_cutoff_sweep}
\caption{Cutoff sweep on all complete frames of the ten SkillCorner
matches (436{,}648 frames). This is not the 150-frame sample
\texorpdfstring{$\tilde{T}$}{} of Section~\ref{sec:data}.
The grid is \texorpdfstring{$\delta\in[0.25, 40.0]$}{}~m at
$0.25$~m. Adopted cutoffs are the cardinality inversion of
Section~\ref{sec:cutoffselection}: $2.75$, $11.75$, and $23.0$~m.
\textbf{(a)}~Pooled mean cluster count (\texorpdfstring{$H_0$}{})
versus \texorpdfstring{$\delta$}{}, with a
\texorpdfstring{$\pm 1$}{} s.d.\ ribbon across the ten match-level
means. Solid vertical lines mark the adopted cutoffs.
\textbf{(b)}~Calinski--Harabasz index, silhouette score, and
information-content on the 1~Hz diagnostic subset, each min-max
scaled to $[0,1]$ for display. The dotted line marks the interior
silhouette local maximum ($7.0$~m). Solid lines repeat the adopted
cutoffs. Quality metrics are not the selector.}
\label{fig:cutoff}
\end{figure}

\subsection{Maximum filtration distance}
\label{sec:adaptivefiltration}

Clustering at cutoff $\delta$ produces a reduced point cloud
$\tilde{P}(\tilde{t})$. Inter-centroid distances on that cloud are typically
larger than $\delta$. A maximum filtration distance
$\varepsilon_{\max}$ fitted to one cutoff can then return no $H_1$ at
another.

We do not modify the Vietoris--Rips complex. We truncate its parameter
at
\begin{equation}
\varepsilon_{\max} = \max\Big(P_{75}\{d(\bar c_i,\bar c_j) : i<j\},\; \max(5.0,\,2\delta)\Big).
\label{eq:adaptive}
\end{equation}
Here $P_{75}$ is the 75th percentile of pairwise inter-centroid
distances on $\tilde{P}(\tilde{t})$. The first term tracks the geometry of the
reduced cloud at the current cutoff. The second term is a floor. It
prevents a degenerate filtration at small $\delta$. It also ensures the
filtration reaches at least the inter-cluster distance regime.

$P_{75}$ is a conventional upper-quartile summary.
Section~\ref{sec:sensitivity} reports an ablation across $P_{50}$
through $P_{95}$. Every tested percentile returns identical $H_1$
totals and frame-presence rates. The choice is therefore a reporting
convention, not an optimisation.

Persistence diagrams are computed with Ripser via Ripser.py
\citep{bauer2021ripser}, with coefficients in $\mathbb{F}_2$. We report
finite $H_1$ bars. At filtration value $0$, $H_0$ of $\tilde{P}(\tilde{t})$
equals the number of centroids. The $H_0$ figures in
Section~\ref{sec:scaleh0} are these counts. We cross-checked all
primary-match diagrams against GUDHI \citep{gudhi2014} and giotto-tda
\citep{tauzin2021}. Birth--death pairs agreed to within numerical
tolerance ($10^{-6}$~m). GUDHI is also used for the bottleneck-distance
and landscape computations reported in
Section~\ref{sec:complementarity}.

\subsection{Closed cycle identification}
\label{sec:cycles}

Each $H_1$ feature corresponds to a 1-cycle in the Vietoris--Rips
complex. To recover a geometric realisation, we build the adjacency
graph of edges whose distances fall within the feature's persistence
interval $[\text{birth},\text{death}]$. We then enumerate closed cycles
of length at least 3 by breadth-first search from each vertex. If that
graph yields no cycle, we fall back to edges with length in
$[\tfrac{1}{2}\text{birth},\,\text{death}]$. Breadth-first search is
used in preference to depth-first search because it prioritises shorter
cycles. Those cycles correspond to minimal enclosing arrangements.
Among the enumerated cycles, the one whose edge distances lie closest
to the midpoint of the persistence interval is selected as the
geometric representative. This is a geometric proxy for the persistence
pair, not a homology generator extracted from the matrix reduction.

\subsection{Statistical tests}
\label{sec:stats}

To test whether $H_1$ detection reflects the arrangement of cluster
centroids rather than merely their number, each frame is compared
against a matched null. For a frame whose clustering yields $k$
centroids, the null draws $k$ points uniformly from the convex hull of
those same centroids. The null therefore matches the frame on both
cardinality and spatial envelope. It randomises only the arrangement.
The truncation of equation~\eqref{eq:adaptive} is recomputed on each
null cloud. Nothing else differs between the two. We use 200 null
replicates per frame. We report the excess of observed over null $H_1$
presence, with 95\% confidence intervals from bootstrap resampling over
matches.

Complementarity of the two $H_1$ levels
(Section~\ref{sec:complementarity}) is assessed by two frame-level
tests. The Spearman rank correlation is computed on total $H_1$
persistence: the sum of death minus birth over all finite $H_1$ bars in
a frame. We also report it on loop counts as a robustness check.
Fisher's exact test is computed on the binary co-occurrence of $H_1$
presence at the two levels. Both statistics carry 95\% confidence
intervals obtained by bootstrap resampling over matches (1{,}000
resamples). The match is the resampling unit. Bottleneck and landscape
distances between the two levels' persistence diagrams are computed
using GUDHI, as described in Section~\ref{sec:adaptivefiltration}.

\subsection{Software and reproducibility}

All analyses were performed in Python~3.11. Persistence diagrams are
computed with Ripser.py~0.6.12 \citep{bauer2021ripser,tralie2018};
bottleneck distances and landscape representations use
GUDHI~3.11.0 \citep{gudhi2014}; giotto-tda~0.6.0 \citep{tauzin2021}
provided independent cross-checks. Supporting scientific Python packages
match the pinned \texttt{requirements.txt} accompanying the manuscript:
NumPy~2.0.2, SciPy~1.13.1, pandas~2.3.2, scikit-learn~1.6.1. Analysis
code, pipeline configuration, and pinned dependencies are included with
the manuscript materials and will be released in a public repository at
arXiv posting (see Data Availability Statement).
